
1. 可以有多个py文件，但程序应该可以打包到一个exe
2. 程序从外部读取两个文件，分别是提示词和api key
3. 程序在没有参数直接打开时显示添加右键菜单按钮，如果已经添加了则显示删除右键菜单。按钮的文本随系统语言切换中英文。
4. 程序提供获取文件目录，获取文件内容两个调用。
5. 如果用户选择的文件中有且仅有一个文本文件，直接在历史记录中假设ai已经执行了一次读取文件内容。
6. ai可以回复一段指令加可选一段描述，或者一段解释表明为什么做不到。
7. ai回复的指令应该在一个框中，框应该有一个运行按钮。
8. 指令可以是powershell或者python
9. 程序在打开时应该是一个横条的窗口，上面是引用的文件，下面是输入框，在用户按下回车后，窗口随ai的回复而加长。